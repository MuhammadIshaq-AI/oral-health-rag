Rewrite the user's latest message as a single, standalone search query about oral or dental health.

Rules:
- Resolve pronouns and references ("it", "that", "they", "the second one") using the conversation.
- Keep the user's meaning. Do not add new symptoms, conditions or advice.
- Use plain words a health website would use (e.g. "wisdom tooth removal recovery").
- If the latest message is already standalone, return it unchanged.
- Output ONLY the query text, no quotes, no explanation.

Conversation:
{history}

Latest message: {question}

Standalone query:
